{
  "name": "AvitoVimeoFunctionelDemo",
  "version": "1.0.0",
  "description": "Application to consume  vimeo api  with functionnel prograamming styles (rxjs)",
  "main": "server/index.js",
  "author": "omar BERRABEH<omar.berrabeh@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "yarn run clean && yarn run build && node build/index ",
    "watch": "yarn run clean && yarn run build && node build/index ",
    "build": "gulp",
    "clean": "gulp clean "
  },
  "dependencies": {
    "express": "^4.14.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-clean": "^0.3.2",
    "gulp-sass": "^3.1.0",
    "gulp-watch": "^4.3.11"
  }
}
